# Folio 0.1 验证记录

2026-09-20，在 Windows 的独立 Chrome for Testing 149.0.7827.55 配置中加载实际 Manifest V3 扩展完成。未修改日常 Chrome 配置，尚未替用户安装到日常浏览器。

## 已完成

- `npm test`：8 项通过。覆盖精确/松标点查询、缩写优先级、显式变形、不合法输入、递归义项和失效引用、跨缓冲区 Unicode、SHA-256、stored/deflate ZIP 及异常 ZIP。
- `npm run test:browser`：32 项检查通过。真实导入本地朗文包：60,465 个词条、364,887 个查询键，全部计数、SHA-256、父词条及义项引用通过校验。
- 正常查词、原词优先与变形关联、`U.`/`U` 和 `v.`/`V`、`run.` 标点回退、词组定位、缺中文回退英文、派生词与父词条跳转。
- 空词库提示、未收录提示、原英文展开、多词条与更多释义、Esc/外部点击关闭、输入框与 contenteditable 保护。
- 既有页面实时采用新快捷键及启用状态；页面内 iframe 查词；明暗适配；390×844 窄窗口及底部选词定位；网页刻意设置的标题/按钮样式没有污染 Shadow DOM 内部。
- 校验失败与失效引用的包被拒绝，原词典仍可用；导入中途关闭页面后原词典保留；再次导入清理中断残留，同包更新只清理该词典旧版本，保留其他词典。
- 设置页重载后词典可用；工具栏页面显示正确状态；验证期间无页面脚本错误，无对外 HTTP/HTTPS 数据请求。
- 词条底部提供固定的齿轮图标入口，保留可访问名称及悬停提示：词典导入后关闭设置页，再从词条打开设置页成功；长词条滚动后入口仍在卡片可视范围内。
- 词条顶部嵌入 Folio 旋纹：已在真实网页验证扩展图片加载，并检查浅色、深色预览的背景融合效果；主图形周围没有底座或矩形贴图边界。截图为 `folio-brand-light.png` 和 `folio-brand-dark.png`。
- 顶部排布按最新反馈收紧：旋纹居左、品牌文字靠右；图标与品牌行高度 28 px，下方留白 9 px，顶部内边距恢复 17 px。相较上一版少占 22 px 纵向空间。已针对性检查浅色、深色截图及图标加载，此轮未重复全量词典测试。
- 修复扩展重载后的旧页面异常：图标资源地址改为初始化时解析，失效提示不再读取扩展资源；查词与设置入口均捕获同步 API 失效并显示刷新提示。`npm run test:reload` 在独立浏览器开启开发者模式，通过真实管理页连续重载三次，验证旧卡片设置入口、重复查词、刷新恢复、后台消息及图标加载，管理页无 manifest/runtime 错误。修复前同一复现脚本捕获旧卡片设置入口访问已失效 runtime 的未处理异常。8 项核心测试同时通过。
- 用户截图中的通用脚本加载失败尚未复现，不能将旧页面失效处理的修复视为已确定其根因；当前版本启动与三次重载均正常。此次验证数据位于 `test-results/reload-verification.json`。
- 旧版单词典元数据自动迁入词典列表，无需重导；改名即时同步已打开的网页词条及设置页预览；导入第二部后可切回原词典，选择更换会更新已打开的词条。重复导入相同词典保留自定义名称，词典列表、名称及当前选择在重载后保持。
- 已目视检查暖纸色、深色、窄窗口、设置页及工具栏页面的实际截图。
- 产品显示名称统一为 Folio。滚动条为 3 px 宽，距卡片右边缘约 2 px，默认隐藏，滚动时显示，停止 650 ms 后以 180 ms 淡出；滚轮、拖动滑块和键盘滚动均已验证，未带动背景页面，显示隐藏也不改变文字宽度。测试显式启用浏览器滚动条显示，避免无头浏览器默认隐藏滚动条漏掉问题。
- 产品图标采用用户指定的图案，按最新反馈使用内置 imagegen 去除旋纹右下的散开灰影，使图形呈现平面印刷效果，仅保留底座外侧投影，再等比导出浏览器尺寸。原图独立保留，其 SHA-256 与上传文件一致；16/32/48/128/256 px 实际尺寸、manifest 和 action 配置、页面 favicon 和品牌图片均已验证，并目视检查 Chrome 扩展管理页、设置页及弹出页截图。图标替换采用单独的浏览器检查；本次词条嵌入又完成 32 项全量浏览器回归。成品及编辑提示保存在 `design/`。
- `npm run package` 仅收集 `extension/` 文件。最终 ZIP 共 20 个文件，不包含词典包、研究数据、原始大图、测试浏览器或开发依赖；Python `zipfile.testzip()` 和 manifest 资源引用检查通过。

## 本机抽样测量

最终一轮全量导入耗时 **29.25 秒**。12 次查询的扩展消息往返耗时为 **2.2–50.3 毫秒**（包括测试驱动开销），仅用于说明本机当前数据规模下的表现，不代表所有设备或长期性能承诺。

原始结果：`test-results/verification.json`。截图：`folio-light.png`、`folio-dark.png`、`folio-narrow.png`、`folio-inset-scrollbar.png`、`folio-scrollbar-idle.png`、`folio-settings.png`、`folio-popup.png`。这些是本地验证产物，不进入插件 ZIP。

## 验证边界

尚未对所有网站、浏览器版本、跨域沙箱框架和操作系统进行覆盖。浏览器内置 PDF、图片文字、Chrome 受保护页面不在首版支持范围。词典源内容未做全书逐条校对；多义词按原词典顺序呈现，不做 AI 语境判定。

快捷键默认为 Enter，可修改；只有源词典明确记录的变形或关联参与查询，不推测任意词形。初版只支持 Folio 标准词典包 v1.0，可保存多部词典，一次选择一部查词，不做多词典结果合并。
