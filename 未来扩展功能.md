# 未来扩展功能

更新日期：2026-09-20

本文记录项目讨论中提出的后续扩展方向。2026-09-20 已按后续决定进入插件本体实施：初版包含 Folio 标准词典包导入、本地查词和 Folio 卡片；其余以下功能暂不纳入初版，后续按实际使用需要再决定优先级。

## 初版边界

产品首先服务于 Chrome 中的英文阅读，核心要求是方便、简洁、美观，使用过程尽量不打断阅读。

目前确定的初版交互与内容：

- 选中一个英文单词后，按一个快捷键显示词典。
- 展示中文释义、音标，以及适合展示的常用词组及其中文含义。
- 不展示例句；发音与 AI 语境解释暂不进入初版。
- 已选择 Folio 的 UI 方向：柔和圆角、清晰的字体层级和适当留白，避免僵硬、繁杂的界面；后续沿此方向细化。

## 1. AI 语境解释

**状态：用户已明确决定留作后续扩展。**

### 期望体验

先展示基础词典释义，随后补充结合当前句子的 AI 解释。参考用户提供的 WPS 翻译案例，两部分分区呈现，基础查词不依赖 AI 结果。

AI 应能够生成简短的语境解释，而不只是从词典中选择或高亮一个释义。必要时解释专业用法、比喻或固定搭配，帮助读懂原句；避免重复音标、词性等已有信息。

### 已讨论的实现思路（尚未定案）

- 提取所选词及所在句子，必要时增加有限的相邻上下文，不默认发送整篇网页。
- 使用经济型模型生成简短解释；词典可以提供参考，但不限制模型只能选择已有词条。
- 词典结果先显示，AI 结果异步补充；避免卡片明显跳动或突然遮挡更多正文。
- 上下文不可用、请求失败或超时时，保留正常的基础查词体验。
- 切换查询目标后，旧请求的结果不得覆盖新词的内容。

### 消费控制与缓存

用户提出在单个页面内暂存 AI 输出，避免重复调用。这是后续设计的重要方向。

- 缓存应区分“单词＋上下文”，不能仅按单词复用。同词在不同句子中可能有不同含义。
- 相同查询命中页面缓存时，直接复用结果，不再请求模型。
- 相同请求仍在进行时，复用该请求，避免重复触发。
- 页面缓存的生命周期、容量，以及页面内容变化时的失效规则待定。
- 可考虑限制输入上下文、输出长度与重试次数，并提供 AI 开关和调用上限。
- 可考虑短暂延迟发起请求，减少快速关闭卡片产生的调用；请求发出后，取消等待不保证免除已发生的服务端费用。

### 后续需要验证

- 经济型模型对实际阅读材料，尤其专业文章的解释质量。
- 真实调用的输入、输出用量、费用及等待时间；模型和服务商尚未选定。
- 普通网页的上下文提取可靠性，以及 PDF、复杂编辑器等特殊场景是否值得适配。
- 云端解释所需的上下文发送说明与 API 凭据配置方式。

此前讨论的价格仅用于判断费用量级，不作为预算承诺；决定实施时应重新核实官方价格，并用真实材料测量。

## 2. 按住快捷键悬停查词

**状态：已提出的后续交互方向，优先级低于初版的选词后按键查词。**

按住某个键后，自动识别鼠标悬停处的单词并显示释义，减少手动选词步骤。

后续需要确定快捷键、悬停触发时机、松键后的卡片行为，以及鼠标移动时如何避免频繁切换和误触。该交互应复用词典卡片；是否同时触发 AI 解释需要单独评估。

## 3. 单词发音

**状态：作为后续扩展保留，初版只展示音标。**

后续可增加点击播放单词发音的能力。音频来源、英美发音选择、响应速度和费用待评估；初版不必为尚未实现的发音功能放置不可用按钮。

## 4. AI 辅助维护个人词库

**状态：用户提出的后续扩展设想，尚未确定自动化程度与实现方案。**

### 用户希望实现的能力

让 Folio 根据实际阅读与查词需要逐渐积累个人词库：

- 遇到原词典未收录的单词时，通过 AI 自动化流程补充词条，之后可以在本地复用。
- 对错误释义进行纠正，对不完整的释义补充遗漏的含义。
- 收录实际阅读中常见、原词典没有收录的词组及其解释。
- 让词库更贴合个人常读领域，例如光学论文、技术文章，而不是只依赖固定词典的覆盖。

### 未收录时的临时入口

用户提出可以在第一次查不到时提供“搜索 Google”，也指出浏览器本身已经可以搜索，因此这一入口的必要性尚待判断。

建议将它作为未命中卡片里的轻量备用操作，点击后使用当前查询词搜索；不自动打开搜索页面，也不依赖 AI。是否只在首次未命中时显示尚未定案；从实际使用考虑，每次查不到时保留入口可能更一致。

### 建议的自动化流程（尚未定案）

1. **发现候选。** 从用户主动查词中的未命中项、用户标记的错误或不完整释义，以及明确选中的词组收集候选。暂不默认扫描整页、持续记录全部阅读内容；重复出现的短语是否自动成为候选另行讨论。
2. **确认查词对象。** 先处理大小写、标点与词形，区分未收录单词、拼写错误、专名、临时组合和稳定词组，避免把所有未命中项直接建成词条。
3. **生成补充草稿。** AI 结合有限上下文及可核实资料，产出结构化的中文义项、词性、领域标签和必要的词组关系；能确定音标时才补充。当前句子的解释与可长期复用的词典义项分别保存。
4. **检查内容与证据。** 校验格式、重复义项、与原词条的冲突及所引用资料是否实际支持结论。不能只凭模型自报的“高置信度”自动认定正确；再调用一次模型也不等于独立证据。
5. **写入个人补充层。** 可考虑在用户开启自动补充后，将满足核验条件的新增义项自动保存；证据不足、涉及删除旧义项或改变原有含义的修改保留为待核对项。具体规则与核验成本待评估。
6. **后续复用与纠错。** 再查时优先复用适用的本地补充，保留查看来源、修改、删除和撤销的入口；遇到新的语境可以增加不同义项，不能把第一次解释强行套用于所有场景。

### 数据组织与使用体验建议

- 将原始词典与个人补充分别保存，通过覆盖或追加规则组合展示。纠错可以改变实际显示结果，同时保留原始版本，方便回退、更新词典或更换来源。
- 记录 AI 补充标记、核验状态、来源与时间、适用领域，以及必要的修改历史。保留上下文的范围、保存期限和页面地址是否记录需要另行决定。
- 自动补充是扩展功能，不应阻塞基础查词或让卡片变成繁杂的词库管理界面；提示与管理入口应保持克制。
- 个人词条长期存储不同于单页面 AI 解释缓存：前者用于跨页面复用词义，后者用于复用同一语境中的解释，两者需要不同的匹配与失效规则。
- 按规范词头、词组和具体补充目标合并任务，避免同一次未命中重复调用；提供调用预算、重试上限和开关。已有条目不代表以后任何语境都无需补充。

### 后续需要验证

- AI 能否稳定识别遗漏与错误，而不把原本正确的词条改坏。
- 专业术语、缩略词、多义词及固定搭配的核验办法。
- 自动写入与待核对之间的边界，能否兼顾可靠性和较少人工操作。
- 检索、生成、核验的合计费用与等待时间，而不只计算生成一段解释的费用。
- 保留来源、词典更新、补充内容导出与原词典授权之间的关系。

## 5. 自行导入词典与可替换的数据来源

**状态：后续讨论已将「自行导入 Folio 标准词典包」纳入初版。任意 MDX 导入、多词典并用等仍是未来方向。**

> 或许Folio可以做这种功能，即词典只是一个可选的来源，也就是说用户可以自己下载词典并导入Folio使用。

这一方向让 Folio 的阅读交互与卡片 UI 能够复用于用户选择的词典，不必永久绑定某一个内置词库；也可以与个人补充层组合使用。

初版采用 Folio 标准词典包 v1.0，支持保存多部词典、自定义名称并选择其中一部查词；新词典加入列表，相同词典包更新对应内容并保留名称。程序不捆绑原词典内容。原始 MDX 通过本地转换工具预处理。多部词典同时查询的优先级、重复义项合并和其他源格式适配仍需后续讨论，尚未承诺直接支持任意 MDX、StarDict。

用户本地导入与项目公开分发词典是不同的使用方式，具体词典仍按其来源和授权条件处理。

## 范围说明

以上方向均未承诺实施时间。多轮 AI 追问、整篇文章分析、长期学习记录等不属于当前已确定的扩展需求；不因讨论过技术可能性而自动纳入项目范围。
