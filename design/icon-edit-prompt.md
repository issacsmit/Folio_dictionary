# 图标局部编辑

工具：内置 imagegen（未使用 CLI/API fallback）。

编辑输入：`folio-icon-original.png`。结果：`folio-icon.png`。

Use case: precise-object-edit. Asset type: Folio app icon, minimal local retouch of the supplied ORIGINAL artwork. Input image is the edit target, not a style reference. Preserve the exact black calligraphic spiral silhouette, all stroke contours and tips, proportions, position, size, cream rounded-square tile, white surrounding canvas, composition and colors. ONLY lighten the muddy dark shadow on the cream tile beneath and to the lower right of the black spiral, approximately x=650..970 y=810..985 in the 1254px source. Reduce this localized shadow's darkness roughly 40 percent and make its falloff smooth and subtle, so it looks like deliberate soft contact shading rather than a dirty generated blotch. Keep a gentle intentional hint of depth; do not erase all shading or flatten the tile. Do not lighten the black logo stroke itself. Keep the outside tile shadow, background, framing, and every other element unchanged. No redesign, no added text, no marks, no cropping. Return the edited icon artwork only, square.
